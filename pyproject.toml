[project]
name = "counterpoint"
version = "0.1.0"
description = "对抗式多空投研:多个 agent 经 Band 协作产出研究备忘录(非交易系统)"
requires-python = ">=3.11"
dependencies = [
    "band-sdk[anthropic,langgraph]",
    "langchain-openai>=1.3.0",
    "python-dotenv",
    "httpx>=0.24.0",
]
